gpasswd groupname
• Sets or changes the group password.
• Allows users to join the group using the group password (rarely used in modern systems).

gpasswd -r groupname
• Removes (deletes) the group password, disabling password-based group access.
